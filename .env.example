# Copy to .env and set a local password before starting Compose.
POSTGRES_PASSWORD=
# Optional: change this if another local database already uses port 5432.
POSTGRES_PORT=5432
# Alpha Vantage daily data; keep the real key only in ignored .env.
ALPHAVANTAGE_API_KEY=
